import React from 'react';
import PropTypes from 'prop-types';

const Main = props => {
    return (
        <div>
                main
        </div>
    );
};

Main.propTypes = {

};

export default Main;
